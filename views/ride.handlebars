<div class="container-fluid">
  <div class="col-md-8">
    <h1>{{ride.destination}}<h1>
    <h3>{{ride.user}}</h3>
    <h3>{{ride.spacesAvailable}}</h3>
  </div>
  <div class="col-md-4">
    <a href="/" class="btn btn-primary">Go Back</a>
    <a href="/ride/{{key}}/delete" class="btn btn-danger">Delete</a>
  </div>
</div>
